---
description: Plan a feature — produces docs/tasks/<feature>/plan.md via the feature-planner agent.
---

Invoke the `feature-planner` agent for the following feature request:

$ARGUMENTS

The planner should read `docs/architecture/principles.md`, locate or request the relevant detailed spec, use the `feature-plan-template` skill, and produce a complete plan. Present it and wait for my approval before handing off to engineers.
