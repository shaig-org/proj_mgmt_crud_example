---
description: Write or update specifications — routes to spec-author.
---

Invoke the `spec-author` agent to work on: $ARGUMENTS

The author should use the `spec-authoring` skill, keep `main_spec.md` as an index only, and write detail to `docs/spec/detailed/`.
