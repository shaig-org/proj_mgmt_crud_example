# Capability verbs and roles

Generated by `python -m project_management_crud_example.tools.dump_capabilities_doc`. Do not edit by hand — re-run the script.

## `ActivityLogReadCapability`

_module: `project_management_crud_example.capabilities.activity_logs_capability`_

Read-only activity log capability.

Cross-org reads are surfaced as not-found (None), consistent with the
existing routers' 404-on-cross-org behavior.

## `CommentReadCapability`

_module: `project_management_crud_example.capabilities.comments_capability`_

Read-side comment authorization. Org-scoped.

## `OrgCommentModerationCapability`

_module: `project_management_crud_example.capabilities.comments_capability`_

Admin moderation: delete any comment in the caller's organization.

Must be constructed via `get_org_comment_moderation_capability`, which
enforces admin role at the DI boundary. No update verb — moderators do
not edit other users' words. Super admins bypass the org check.

## `OwnCommentWriteCapability`

_module: `project_management_crud_example.capabilities.comments_capability`_

Create / update-own / delete-own comments. `author_id` is baked in.

Methods do NOT accept an `author_id` parameter. `update` and `delete_own`
verify the target comment's author matches the captured user and raise
otherwise. A structural test in tests/capabilities keeps this invariant
honest.

## `EpicReadCapability`

_module: `project_management_crud_example.capabilities.epics_capability`_

Read-side epic authorization.

## `OrgEpicWriteCapability`

_module: `project_management_crud_example.capabilities.epics_capability`_

Write-side epic authorization.

## `GlobalOrganizationWriteCapability`

_module: `project_management_crud_example.capabilities.organizations_capability`_

Create / update / delete organizations.

Scope is GLOBAL (super-admin only). Must be constructed via
`get_global_organization_write_capability`, which enforces the role at
the DI boundary so non-super-admins cannot even construct it.

## `OrganizationReadCapability`

_module: `project_management_crud_example.capabilities.organizations_capability`_

Read-side organization authorization.

## `PasswordChangeCapability`

_module: `project_management_crud_example.capabilities.password_capability`_

Change the current user's own password. No other writes permitted.

## `BoundProjectWriteCapability`

_module: `project_management_crud_example.capabilities.projects_capability`_

Project write operations scoped to a specific, already-authorized project.

Verbs and roles:
  - update:    SUPER_ADMIN, ADMIN, PROJECT_MANAGER
  - archive:   SUPER_ADMIN, ADMIN, PROJECT_MANAGER
  - delete:    SUPER_ADMIN, ADMIN
  - unarchive: SUPER_ADMIN, ADMIN

Scope: the project passed to `OrgProjectWriteCapability.bind()`. Role and
cross-org checks have already run by the time this object exists; verbs
re-check the role defensively (cheap, preserves defense-in-depth).

Encapsulation rule: the only public state is `current` — a snapshot of the
project as fetched at bind-time, used for diff logging and for the route's
response shape. The repository and current user are intentionally not
exposed; verbs absorb every side-effect a route used to perform via
`cap.repo` / `cap.user`.

`current` is the pre-mutation snapshot; verbs return the post-mutation
`Project` (caller may shadow `current` if they want the new state, but the
bound cap itself does not mutate `current`).

## `OrgProjectWriteCapability`

_module: `project_management_crud_example.capabilities.projects_capability`_

Org-scoped project write capability (collection-level).

Verbs and roles:
  - build_create_command: SUPER_ADMIN, ADMIN, PROJECT_MANAGER
  - create:               SUPER_ADMIN, ADMIN, PROJECT_MANAGER
  - bind:                 SUPER_ADMIN, ADMIN, PROJECT_MANAGER
                          (returns a `BoundProjectWriteCapability` for a
                          specific project; per-verb role checks happen on
                          the bound capability)

Scope: the user's organization (cross-org access rejected; super-admins
bypass cross-org).

Encapsulation rule: the underlying `Repository` and current `User` are
private (`_repo`, `_user`). Routes do not access them — every side-effect
a route needs (e.g. activity logging on create) is absorbed into the verb.

## `ProjectReadCapability`

_module: `project_management_crud_example.capabilities.projects_capability`_

Read-side project authorization.

## `OrgTicketWriteCapability`

_module: `project_management_crud_example.capabilities.tickets_capability`_

Write-side ticket authorization.

## `TicketReadCapability`

_module: `project_management_crud_example.capabilities.tickets_capability`_

Read-side ticket authorization.

## `OrgUserWriteCapability`

_module: `project_management_crud_example.capabilities.users_capability`_

Write access to any user in the caller's organization.

Must be constructed via `get_org_user_write_capability`, which enforces
admin role at the DI boundary. Super admins bypass the org check.

## `SelfUserWriteCapability`

_module: `project_management_crud_example.capabilities.users_capability`_

Write access to EXACTLY the authenticated user's own record.

The target user id is captured at construction and is not a method
parameter. No method on this class accepts `user_id`.

## `UserReadCapability`

_module: `project_management_crud_example.capabilities.users_capability`_

Read-side user authorization.

## `OrgWorkflowWriteCapability`

_module: `project_management_crud_example.capabilities.workflows_capability`_

Write-side workflow authorization.

## `WorkflowReadCapability`

_module: `project_management_crud_example.capabilities.workflows_capability`_

Read-side workflow authorization.
